# PyTorch nightly with CUDA 12.8 (RTX 5090 sm_120 support)
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch
torchvision
torchaudio

# GPU monitoring and management
pynvml>=11.450.129
gpustat

# Project dependencies (expand as needed in later phases)
python-dotenv

# Video processing
ffmpeg-python>=0.2.0

# Speech recognition (Phase 3)
faster-whisper>=1.0.0  # Optimized Whisper with 2-4x speedup and built-in VAD
