---
phase: 08-quality-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/__init__.py
  - src/ui/app.py
  - src/app.py
autonomous: true

must_haves:
  truths:
    - "Gradio app launches at localhost:7860 without errors"
    - "Four-step workflow sections (Upload, Review, Processing, Output) are visible in correct order"
    - "Upload section shows video upload widget and HuggingFace token field"
    - "Review section contains an editable transcript dataframe and preview controls"
    - "Processing section shows status text and a Cancel button"
    - "Output section shows a video player"
  artifacts:
    - path: "src/ui/__init__.py"
      provides: "Package marker for src.ui"
    - path: "src/ui/app.py"
      provides: "Full Gradio Blocks UI layout with all four workflow sections"
      min_lines: 120
      contains: "gr.Blocks"
    - path: "src/app.py"
      provides: "Entry point that imports from src.ui.app and calls demo.launch()"
      contains: "from src.ui.app import demo"
  key_links:
    - from: "src/app.py"
      to: "src/ui/app.py"
      via: "import statement"
      pattern: "from src\\.ui\\.app import demo"
    - from: "src/ui/app.py"
      to: "gr.Blocks"
      via: "context manager"
      pattern: "with gr\\.Blocks"
---

<objective>
Create the Gradio web UI scaffold for Voice Dub: a four-step workflow layout (Upload -> Review Transcript -> Processing -> Output) with all UI components in place but event handlers stubbed.

Purpose: Establishes the complete UI structure that pipeline_runner.py (Plan 03) and event wiring (Plan 04) will connect to. The scaffold must define every component that later plans reference.

Output: src/ui/app.py (Gradio Blocks layout), src/ui/__init__.py (package marker), updated src/app.py (entry point routing to new UI).
</objective>

<execution_context>
@C:\Users\ASBL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ASBL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quality-controls/08-RESEARCH.md
@src/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui package with Gradio Blocks scaffold</name>
  <files>src/ui/__init__.py, src/ui/app.py</files>
  <action>
Create src/ui/__init__.py as an empty package marker.

Create src/ui/app.py with the full Gradio 6.6.0 Blocks layout. The file must:

1. Import: gradio as gr, threading, pandas as pd, pathlib.Path
2. Declare module-level: `_cancel_event = threading.Event()` and `_pipeline_event = None`
3. Define `create_app() -> gr.Blocks` that builds and returns the demo object. Use `with gr.Blocks(title="Voice Dub", theme=gr.themes.Soft()) as demo:` inside this function.

Inside create_app(), build four gr.Column sections with visibility toggled by a gr.State:

**Step 1 - Upload (visible=True):**
- gr.Markdown("## Step 1: Upload Video")
- video_input = gr.Video(label="Source Video", sources=["upload"], interactive=True)
- hf_token_input = gr.Textbox(label="HuggingFace Token (required for speaker diarization)", type="password", placeholder="hf_...")
- video_info_text = gr.Textbox(label="Video Info", interactive=False, lines=5)
- start_asr_btn = gr.Button("Extract & Transcribe", variant="primary", size="lg")
- asr_status = gr.Textbox(label="Status", interactive=False, lines=2, visible=False)

**Step 2 - Review Transcript (visible=False):**
- gr.Markdown("## Step 2: Review & Edit Transcript")
- gr.Markdown("Edit the **Text** column to fix transcription errors. Other columns are read-only.")
- transcript_table = gr.Dataframe(
    headers=["ID", "Speaker", "Start", "End", "Text", "Confidence"],
    datatype=["number", "str", "number", "number", "str", "number"],
    interactive=True,
    static_columns=[0, 1, 2, 3, 5],
    wrap=True,
    label="Transcript (edit Text column only)",
    row_count=(1, "dynamic"),
  )
- gr.Markdown("### Preview a 30-second clip before full render:")
- with gr.Row(): preview_start_slider = gr.Slider(minimum=0, maximum=600, value=0, step=1, label="Preview start (seconds)") and preview_btn = gr.Button("Preview 30s Clip", variant="secondary")
- preview_video = gr.Video(label="Preview Clip", visible=False)
- proceed_btn = gr.Button("Start Full Dubbing", variant="primary", size="lg")
- back_to_upload_btn = gr.Button("Back to Upload", variant="secondary")

**Step 3 - Processing (visible=False):**
- gr.Markdown("## Step 3: Processing")
- stage_status_text = gr.Textbox(label="Current Stage", interactive=False, lines=3)
- progress_bar = gr.Progress()  -- NOTE: gr.Progress() is passed as a function arg, not a component. Instead use: overall_progress = gr.Slider(minimum=0, maximum=100, value=0, label="Overall Progress", interactive=False)
- cancel_btn = gr.Button("Cancel Processing", variant="stop", size="lg")

**Step 4 - Output (visible=False):**
- gr.Markdown("## Step 4: Complete")
- output_video = gr.Video(label="Dubbed Video")
- download_info = gr.Textbox(label="Output Info", interactive=False, lines=3)
- process_another_btn = gr.Button("Process Another Video", variant="secondary")

Store all component references as local variables in create_app() so they can be used in .click()/.change() bindings later in Plan 04. Return demo.

Add at module level after create_app(): `demo = create_app()`

CRITICAL: Do NOT use `queue=False` or event handler bindings yet — those go in Plan 04. This plan only creates the layout.

NOTE on gr.Dataframe row_count: In Gradio 6, use `row_count=(1, "dynamic")` — NOT a plain integer or tuple with 3 items.
  </action>
  <verify>
Run: `cd "C:\Users\ASBL\Desktop\Voice Dub" && python -c "from src.ui.app import demo; print('UI loaded:', type(demo).__name__)"`
Expected: prints "UI loaded: Blocks" with no import errors.
  </verify>
  <done>src/ui/app.py imports without error and exposes a gr.Blocks instance as `demo`. All four workflow sections defined with correct components.</done>
</task>

<task type="auto">
  <name>Task 2: Update src/app.py entry point to route to new UI</name>
  <files>src/app.py</files>
  <action>
Replace the content of src/app.py entirely. The existing file is a Phase 2 placeholder (simple Gradio app for video roundtrip test). Phase 8 replaces it with the full quality-control UI.

New src/app.py content:
```python
"""
Voice Dub - Main entry point.
Launches the full quality-controls web UI (Phase 8).
"""
from pathlib import Path
import sys

# Add project root to Python path
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))

from src.ui.app import demo

if __name__ == "__main__":
    demo.launch(
        server_name="0.0.0.0",
        server_port=7860,
        max_file_size="500mb",
        share=False,
    )
```

Do NOT import or reference any Phase 2 video_processing functions. The old display_video_info and process_video_ui functions are discarded — Phase 8 builds the full pipeline UI instead.
  </action>
  <verify>
Run: `cd "C:\Users\ASBL\Desktop\Voice Dub" && python src/app.py` — Gradio should start and print "Running on local URL: http://0.0.0.0:7860". Stop it with Ctrl+C after confirming startup message appears.
  </verify>
  <done>src/app.py launches Gradio on port 7860. The full four-step UI is accessible at http://localhost:7860. No import errors.</done>
</task>

</tasks>

<verification>
1. `python -c "from src.ui.app import demo; print(type(demo).__name__)"` prints "Blocks"
2. `python src/app.py` starts without errors (Ctrl+C to stop)
3. `python -c "import gradio as gr; import src.ui.app"` has no import errors
4. src/ui/__init__.py exists
5. src/ui/app.py contains "gr.Blocks", "gr.Dataframe", "gr.Video", "gr.Textbox", "gr.Button", "gr.Slider"
6. src/app.py contains "from src.ui.app import demo"
</verification>

<success_criteria>
- src/ui/ package exists with app.py and __init__.py
- Gradio app loads in Python without errors
- Four workflow sections defined (upload, review, processing, output)
- src/app.py routes to new UI (old Phase 2 placeholder replaced)
- All components (transcript_table, preview_video, cancel_btn, output_video, etc.) defined as named variables
</success_criteria>

<output>
After completion, create `.planning/phases/08-quality-controls/08-01-SUMMARY.md`
</output>
