---
phase: 03-speech-recognition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/audio_preprocessing.py
  - src/stages/__init__.py
  - src/stages/transcription.py
  - src/config/settings.py
autonomous: true

must_haves:
  truths:
    - "Audio files are converted to 16kHz mono WAV before ASR processing"
    - "Whisper Large V3 transcribes audio with word-level timestamps"
    - "Detected language is captured from Whisper auto-detection"
    - "VAD filtering prevents hallucinations on silent segments"
  artifacts:
    - path: "src/utils/audio_preprocessing.py"
      provides: "FFmpeg-based audio preprocessing to 16kHz mono WAV"
      exports: ["preprocess_audio_for_asr"]
    - path: "src/stages/transcription.py"
      provides: "Whisper-based transcription with word timestamps"
      exports: ["transcribe_audio", "TranscriptionResult"]
    - path: "src/stages/__init__.py"
      provides: "Stages package initialization"
  key_links:
    - from: "src/stages/transcription.py"
      to: "src/models/model_manager.py"
      via: "ModelManager for sequential GPU loading"
      pattern: "manager\\.load_model"
    - from: "src/stages/transcription.py"
      to: "src/utils/audio_preprocessing.py"
      via: "Audio preprocessing before transcription"
      pattern: "preprocess_audio_for_asr"
---

<objective>
Implement audio preprocessing and Whisper-based transcription with word-level timestamps.

Purpose: Provides the foundation for speech recognition - audio must be preprocessed to 16kHz mono for both Whisper and pyannote, and Whisper transcription produces word-level timestamps required for lip sync accuracy (0.1s requirement from Phase 7).

Output: Audio preprocessing utility and transcription module that loads Whisper Large V3 via ModelManager, transcribes with word timestamps, and returns structured results with language detection.
</objective>

<execution_context>
@C:\Users\ASBL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ASBL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-speech-recognition/03-RESEARCH.md
@src/models/model_manager.py
@src/config/settings.py
@src/video_processing/extractor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audio preprocessing utility for ASR</name>
  <files>src/utils/audio_preprocessing.py</files>
  <action>
Create audio preprocessing utility that converts any audio format to 16kHz mono PCM WAV required by Whisper and pyannote.

Implementation requirements:
1. Create `preprocess_audio_for_asr(input_path: str, output_path: str = None) -> str` function
2. Use ffmpeg-python (already installed) for conversion:
   - acodec='pcm_s16le' (16-bit signed PCM)
   - ar=16000 (16kHz sample rate - REQUIRED by Whisper)
   - ac=1 (mono channel - REQUIRED by both Whisper and pyannote)
   - f='wav' (WAV container)
3. If output_path is None, generate output in same directory as input with `_16khz_mono.wav` suffix
4. Create parent directories if needed (Path.mkdir(parents=True, exist_ok=True))
5. Use overwrite_output=True for ffmpeg
6. Include proper error handling - catch ffmpeg.Error and re-raise with descriptive message
7. Add docstring explaining why 16kHz mono is required (both Whisper and pyannote expect this format)
8. Return the output path as string

Follow the existing pattern from src/video_processing/extractor.py for FFmpeg error handling.
  </action>
  <verify>
Run: python -c "from src.utils.audio_preprocessing import preprocess_audio_for_asr; print('Import successful')"
Expected: Import successful, no errors
  </verify>
  <done>
preprocess_audio_for_asr function exists, imports correctly, and converts audio to 16kHz mono WAV format using FFmpeg
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Whisper transcription module with word timestamps</name>
  <files>src/stages/__init__.py, src/stages/transcription.py, src/config/settings.py</files>
  <action>
Create transcription module using faster-whisper with word-level timestamps and VAD filtering.

1. Create src/stages/__init__.py with empty content (package marker)

2. Create src/stages/transcription.py with:

a) Import requirements:
   - from faster_whisper import WhisperModel
   - from src.models.model_manager import ModelManager
   - from src.utils.audio_preprocessing import preprocess_audio_for_asr
   - from src.config.settings import MODELS_DIR
   - dataclasses for TranscriptionResult and WordInfo

b) Create dataclasses:
   - WordInfo: word (str), start (float), end (float), probability (float)
   - SegmentInfo: id (int), text (str), start (float), end (float), words (List[WordInfo]), avg_logprob (float)
   - TranscriptionResult: language (str), language_probability (float), duration (float), segments (List[SegmentInfo])

c) Create `transcribe_audio(audio_path: str, model_manager: ModelManager = None) -> TranscriptionResult` function:
   - If model_manager is None, create new one (allows reuse across pipeline)
   - Preprocess audio to 16kHz mono using preprocess_audio_for_asr()
   - Load Whisper via ModelManager: WhisperModel("large-v3", device="cuda", compute_type="float16", download_root=str(MODELS_DIR))
   - Transcribe with: word_timestamps=True, vad_filter=True, beam_size=5, language=None (auto-detect)
   - Convert segments generator to list (needed for multiple iterations)
   - Build TranscriptionResult from segments with WordInfo for each word
   - DO NOT unload model (leave that to caller or next model load) - this allows chaining with diarization
   - Return TranscriptionResult

d) Include progress print statements: "Loading Whisper Large V3...", "Transcribing audio...", "Transcription complete: X segments, language: Y"

3. Update src/config/settings.py:
   - Add ASR_SAMPLE_RATE = 16000 (required by Whisper/pyannote)
   - Add ASR_CONFIDENCE_THRESHOLD = 0.7 (for low-confidence flagging in next plan)

CRITICAL: Use word_timestamps=True (not return_timestamps="word") - this is faster-whisper API, not transformers.
CRITICAL: Use vad_filter=True to prevent hallucinations on silence.
CRITICAL: Use compute_type="float16" to halve VRAM usage (~4.5GB vs ~10GB for float32).
  </action>
  <verify>
Run: python -c "from src.stages.transcription import transcribe_audio, TranscriptionResult, WordInfo, SegmentInfo; print('Import successful')"
Expected: Import successful, no errors
  </verify>
  <done>
Transcription module exists with transcribe_audio function that loads Whisper Large V3 via ModelManager, uses word-level timestamps and VAD filtering, and returns TranscriptionResult dataclass with segments containing word-level timing
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run import verification:
   ```
   python -c "from src.utils.audio_preprocessing import preprocess_audio_for_asr; from src.stages.transcription import transcribe_audio, TranscriptionResult; print('All imports successful')"
   ```
   Expected: "All imports successful"

2. Verify settings updated:
   ```
   python -c "from src.config.settings import ASR_SAMPLE_RATE, ASR_CONFIDENCE_THRESHOLD; print(f'ASR sample rate: {ASR_SAMPLE_RATE}, threshold: {ASR_CONFIDENCE_THRESHOLD}')"
   ```
   Expected: "ASR sample rate: 16000, threshold: 0.7"

3. Verify module structure:
   ```
   python -c "from src.stages.transcription import WordInfo, SegmentInfo, TranscriptionResult; print(f'Dataclasses: {WordInfo.__name__}, {SegmentInfo.__name__}, {TranscriptionResult.__name__}')"
   ```
   Expected: Shows all three dataclass names
</verification>

<success_criteria>
- Audio preprocessing utility converts to 16kHz mono WAV using FFmpeg
- Transcription module loads Whisper Large V3 via ModelManager (sequential loading pattern)
- Word-level timestamps enabled (word_timestamps=True)
- VAD filtering enabled to prevent hallucinations (vad_filter=True)
- Language auto-detection working (language=None)
- TranscriptionResult dataclass contains segments with word-level timing
- All imports work without errors
- Settings include ASR_SAMPLE_RATE and ASR_CONFIDENCE_THRESHOLD
</success_criteria>

<output>
After completion, create `.planning/phases/03-speech-recognition/03-01-SUMMARY.md`
</output>
